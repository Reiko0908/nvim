return {
  url = "https://codeberg.org/andyg/leap.nvim",
  name = "leap.nvim", -- Adding 'name' is recommended when using raw URLs in Lazy
  config = function()
    -- keep your existing config exactly the same
  end,
}
